function pxtgManipulate(pText)
{
	var result = "I manipulate this string by external js... " + pText;

	console.log("pxtgManipulate (external GitHub js) || input: " + pText);
	console.log("pxtgManipulate (external GitHub js) || output: " + result);

	return result;
}
